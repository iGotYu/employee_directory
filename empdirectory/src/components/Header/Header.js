import React from "react";
import "./style.css";

function Header() {
    return(
<header>
    <h1>Employee Directory </h1>
    <p>
        Click on column headers to filter by table heading or use the search box to narrow your results
    </p>
</header>
    );
}

export default Header;